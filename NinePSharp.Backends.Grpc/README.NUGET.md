# NinePSharp.Backends.Grpc

gRPC backend plugin for exposing gRPC interactions via 9P file operations.

## Install
```bash
dotnet add package NinePSharp.Backends.Grpc
```

Use when service contracts are gRPC-first and you want unified 9P access.
