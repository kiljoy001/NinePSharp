# NinePSharp.Backends.Websocket

WebSocket backend plugin for bridging 9P operations to WebSocket services.

## Install
```bash
dotnet add package NinePSharp.Backends.Websocket
```

Useful for event-driven or bidirectional service integrations behind a 9P interface.
