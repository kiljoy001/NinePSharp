# NinePSharp.Backends.Rest

REST backend plugin for mapping 9P read/write flows to HTTP APIs.

## Install
```bash
dotnet add package NinePSharp.Backends.Rest
```

Use when your upstream integration is REST/HTTP but consumers prefer filesystem semantics.
