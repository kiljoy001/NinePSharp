# NinePSharp.Backends.Mqtt

MQTT backend plugin for publish/subscribe integrations through 9P paths.

## Install
```bash
dotnet add package NinePSharp.Backends.Mqtt
```

Use this package to represent MQTT topics and payload workflows as filesystem operations.
